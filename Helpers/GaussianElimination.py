def GaussianElimination(A, b, mod):
    # This function should perform Gaussian elimination on the matrix A and vector b modulo mod
    # and return the solution vector x
    ax = []
    b = 1
    for i in a:
        if i[0] == 1:
            ax.append(i)
            b *= i[1]
    return b ** 2